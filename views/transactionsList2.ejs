<!DOCTYPE html>
<html>
<%- include('./partials/style.ejs') %>

  <body>
    <%- include('./partials/header.ejs') %>

      <div id="container">
        <main class="column" id="center">
          <article>
            <h3>Here are all of your transactions </h3>



            <a href="/transactions/add/add_transaction" class="btn btn-danger">Click here to add a transaction!</a>
            <% db=0 %>
              <% if (transactions.length> 0) { %>
                <% transactions.forEach(transactions=> { %>
                  <div class="card mt-4">
                    <div class="card-body">
                      <h4 class="card-title">

                        <%= transactions._symbol[0].Description %>


                          <!-- <%= transactions.Date %>  -->


                          <!-- <%= prices[db] %> -->
                      </h4>
                      <div class="card-text mb-2">
                        <% if (result[db]> 0) { %>
                          <h4>Value of the position: +<%= result[db] %> % </h4>
                          <% } else if (result[db] < 0){ %>
                            <h4>Value of the position: <%= result[db] %> % </h4>
                            <% } else { %>
                              <h4>Value of the position: +<%= result[db] %> % </h4>
                              <% } %>

                      </div>

                      <!-- quantity= piece -->

                      <a href="/transactions/<%= transactions._id %>" class="btn btn-success">Read More</a>
                      <!-- <a href="/asd/<%= transactions.id %>" class="btn btn-warning">Edit</a> -->
                      <!-- <form action="/articles/<%= transactions.id %>?_method=DELETE" method="POST" class="d-inline">
                      <button type="submit" class="btn btn-danger">Delete</button>
                    </form> -->
                    </div>
                  </div>

                  <% db=db+1 %>
                    <% })}else {%>
                      <h4>You don't have any transactions!
                      </h4>
                      <!-- <a href="/transactions/add/add_transaction" class="btn btn-success">Click here to add a -->
                        <!-- transaction!</a> -->
                      <% }%>

          </article>
        </main>
        <%- include('./partials/menu.ejs')%>


      </div>
      <%- include('./partials/footer.ejs')%>
  </body>

</html>




</body>

</main>
</div>

</html>